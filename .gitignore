# Pipeline working data — never commit.
# raw/ holds verbatim source payloads (some feeds are 8-11 MB each, daily).
raw/
db/
out/
logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets — the Bookwhen feed token and any Postgres/API credentials
.env
.env.*
!.env.example

# macOS
.DS_Store
