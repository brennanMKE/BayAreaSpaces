# 0002 — Sequoia Fabrica Bookwhen feed token is missing, leaving the last registry TODO

| | |
|---|---|
| **Status** | open |
| **Module** | registry |
| **Platform** | All |
| **First seen** | 2026-08-05 |

## Description

`sequoia-fabrica`'s `bookwhen-public` source is the only remaining `TODO` in the registry. Bookwhen's public iCal feed needs a private token that is only visible inside their admin panel, so it cannot be discovered from outside.

## Expected behavior

`sources.yaml` carries a working feed URL of the form `webcal://feeds.bookwhen.com/ical/lcqebfpp6u7h/{TOKEN}/public.ics?historic_month_count=1`, marked `verified: true` after a real fetch. The token itself lives in `.env` as `SEQUOIA_FABRICA_BOOKWHEN_TOKEN`, not in the committed registry.

## Actual behavior

`url: TODO`. The `bookwhen_html` fallback (issue 0024) covers the same events by parsing the public agenda page, but the ICS is the better source.

## Notes

The account segment `lcqebfpp6u7h` is already confirmed, recovered from the public page's `data-options` blob. Only the token is missing.

Brennan is a founding member — fastest route is the `#events` Slack channel, not email. Admin path: Bookwhen admin → "Calendar feeds".

Research established the host and path shape without ever guessing a live URL, by probing with a deliberately invalid token and observing `404 text/calendar`. Do not deviate from that: a guessed feed URL fails silently as an empty calendar.

Full detail in `spaces/sequoia-fabrica.md`.
